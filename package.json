{
  "name": "astro-railway",
  "private": true,
  "type": "module",
  "scripts": {
    "railway:build": "npm --prefix client ci --include=dev && npm --prefix server ci --omit=dev && npm --prefix client run build && node scripts/copy-client-build.mjs",
    "start:railway": "node scripts/start-railway.mjs",
    "start": "npm run start:railway"
  },
  "engines": {
    "node": ">=20"
  }
}
